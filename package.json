{
  "name": "poliscope",
  "private": true,
  "version": "0.1.0",
  "description": "Le scope politique français — suivez l'Assemblée en temps réel",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "adm-zip": "^0.5.17"
  }
}
